<!DOCTYPE html>
<html lang="en">
<head>
    <title>Todo List App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0f172a;
            color: #e5e7eb;
            margin: 0;
            padding: 0;
        }

        h1 {
            background-color: #2563eb;
            color: white;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        ul {
            list-style: none;
            padding: 20px;
            max-width: 700px;
            margin: auto;
        }

        ul li {
            background: #1e293b;
            border-radius: 6px;
            margin-bottom: 12px;
            padding: 15px;
            position: relative;
        }

        .delete-btn {
            color: #ef4444;
            position: absolute;
            right: 15px;
            top: 15px;
            text-decoration: none;
            font-weight: bold;
        }

        .edit-btn {
            color: #facc15;
            position: absolute;
            right: 45px;
            top: 15px;
            text-decoration: none;
            font-weight: bold;
        }

        form {
            background: #1e293b;
            padding: 20px;
            max-width: 700px;
            margin: 20px auto;
            border-radius: 6px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 4px;
            border: none;
        }

        input[type="submit"] {
            background-color: #22c55e;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
        }

        input[type="submit"]:hover {
            background-color: #16a34a;
        }
    </style>
</head>

<body>
    <h1>hii guys this side priyanshu sample application for testing purposes</h1>

    <ul>
        <% todolist.forEach(function(todo, index) { %>
            <li>
                <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
                <a href="/todo/<%= index %>" class="edit-btn">✎</a>
                <%- todo %>
            </li>
        <% }); %>
    </ul>

    <form action="/todo/add/" method="post">
        <label for="newtodo">What should I do?</label>
        <input type="text" name="newtodo" id="newtodo" autofocus />
        <input type="submit" value="Add" />
    </form>
</body>
</html>

